# AIクエリプロンプト：HARTAセグメンテーション結果が全体マスクになる問題

## 問題の概要
HARTAを使用したEAT（心外膜脂肪組織）セグメンテーションで、出力が正しいバイナリマスクではなく、**ほぼ全ピクセルが1になった全体マスク**になっています。座標系は正しく保持されているが、セグメンテーション自体が失敗しています。

## 分析結果の詳細

### 1. HARTA PNG出力の分析結果

#### fat/ディレクトリ（重要）
- **ファイル形式**: RGBA PNG (512×512×4)
- **問題点**: RGBチャンネル（0,1,2）の値が**0または255の2値のみ**
- **マスクの振る舞い**:
  - 一部のスライス（例: slice 0）: 26.17%の占有率
  - 多くのスライス（例: slice 100, 145, 190）: **0%の占有率**（完全に空）
  - その他のスライス: 18-25%程度の占有率

#### 最終NIfTI分析（致命的問題）
```
セグメンテーション統計:
  ユニーク値: [1.]  ← 0がなく、1のみ！
  非ゼロボクセル: 56885248 / 56885248
  占有率: 100.00%  ← 全ボクセルが1
  総ボリューム: 48247.24 ml  ← 異常に大きい
```

### 2. 問題の原因分析

#### 問題のあるコード部分

**harta_masks_to_nifti_preserve_geom.py**の`load_mask_from_png`関数:
```python
def load_mask_from_png(png_path):
    im = Image.open(png_path).convert("RGBA")
    arr = np.array(im)  # (H, W, 4)
    
    # アルファチャンネル使用
    alpha = arr[..., 3]
    if alpha.max() > 0:
        mask = (alpha > 0).astype(np.uint8)  # ← 問題: アルファは常に255
    else:
        mask = (arr[..., 0:3].sum(axis=-1) > 0).astype(np.uint8)
    return mask
```

**問題点**:
1. HARTAのPNGファイルは**アルファチャンネルが常に255**（完全不透明）
2. そのため`alpha > 0`は常にTrue → **全ピクセルが1になる**
3. 本来はRGBチャンネルの値（0 or 255）でマスク判定すべき

### 3. HARTAの実際の出力形式

HARTAのfat/ディレクトリのPNG分析:
- **RGB値が255の場所**: EATとして検出された領域
- **RGB値が0の場所**: 背景
- **アルファチャンネル**: 常に255（不透明）

例：
```
CTRATE001_0_fat.png:
  チャンネル0(R): ユニーク値=[0, 255], 非ゼロ=68607
  チャンネル1(G): ユニーク値=[0, 255], 非ゼロ=68607  
  チャンネル2(B): ユニーク値=[0, 255], 非ゼロ=68607
  チャンネル3(A): ユニーク値=[255], 非ゼロ=262144  ← 全ピクセル
```

## 質問

### メイン質問
HARTAが出力するRGBA形式のPNGマスクファイルから正しくバイナリマスクを抽出する方法を教えてください。アルファチャンネルではなくRGBチャンネルの値でマスク判定する必要があります。

### 具体的な実装要求

#### 1. 正しいマスク抽出ロジック
以下の条件でHARTA PNGからバイナリマスクを作成する方法:
- RGBA形式のPNG（アルファは常に255）
- RGBチャンネルの値: 0（背景）または255（マスク）
- 複数のスライスで一部が完全に空（全ピクセル0）

#### 2. 修正コード例
```python
def load_mask_from_png(png_path):
    """HARTA PNG（RGBA）から正しくマスクを抽出"""
    # TODO: 正しい実装を提供してください
```

#### 3. エッジケースの処理
- 完全に空のスライス（RGB全て0）
- 部分的にマスクがあるスライス
- マスクの値が0/255以外の場合の処理

### 追加の考慮事項

#### HARTAの動作仕様
- 非深層学習ベースの古典的手法
- 心臓領域を検出してから脂肪HU値（-190〜-30）でフィルタリング
- 出力はRGBA PNGで、RGBが実際のマスク情報を持つ

#### 期待される結果
- EATボリューム: 通常100-300ml程度（48247mlは異常）
- マスク占有率: 画像全体の1-5%程度
- バイナリ値: 0（背景）と1（EAT）のみ

## 参考情報

### 現在の処理フロー
1. NIfTI → DICOM変換（座標系保持済み）✓
2. HARTA実行（PNG出力生成）✓
3. PNG → 3D NIfTI変換（**ここが問題**）✗
4. 座標系検証（一致確認済み）✓

### 環境
- Python 3.12
- SimpleITK 2.5.2
- PIL/Pillow 11.3.0
- numpy 2.2.6
- HARTA: 非DLベース、Python 3.8.3推奨

## 期待する解決策

1. **即座に使える修正版の`load_mask_from_png`関数**
2. **HARTA PNG形式の正確な仕様理解**
3. **バイナリマスク抽出のベストプラクティス**

これらについて、医療画像処理の標準的な手法とHARTA固有の出力形式を考慮した解決策を教えてください。